/**
 * JobDescription slice state types.
 * Extracted from src/store/slices/jobDescriptionSlice.ts (no content changes).
 */
export interface JobDescriptionState {
  job_description: string;
  company_context: string;
}
